import React from "react";
import "./Skills.css";

const Skills = ({ sectionState }) => {
  return (
    <section className={`skills_container ${sectionState}`}>Skills</section>
  );
};

export default Skills;
