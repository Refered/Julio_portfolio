import React from "react";
import "./Portfolio.css";

const Portfolio = ({ sectionState }) => {
  return (
    <section className={`portfolio_container ${sectionState}`}>
      Portfolio
    </section>
  );
};

export default Portfolio;
