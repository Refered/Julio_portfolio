import React from "react";
import "./Contact.css";

const Contact = ({ sectionState }) => {
  return (
    <section className={`contact_container ${sectionState}`}>Contact</section>
  );
};

export default Contact;
